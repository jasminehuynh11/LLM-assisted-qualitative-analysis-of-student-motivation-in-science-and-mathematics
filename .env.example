# Copy this file to ".env" in this folder and add your keys (never commit .env).
# Default: OpenAI — set OPENAI_API_KEY for python code_responses.py

OPENAI_API_KEY=

# Only if you use: python code_responses.py --provider gemini
GEMINI_API_KEY=
